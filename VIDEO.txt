Ajaia Docs — Walkthrough Video
================================

Add your unlisted Loom or YouTube link below after recording the 3–5 minute
walkthrough:

VIDEO URL: <paste your unlisted Loom / YouTube link here>

Suggested outline (3–5 min):
  1. Main user flow — sign in, create/rename a doc, format text, autosave.
  2. File upload — import samples/sample-import.md into a new document.
  3. Sharing — share with bob@ajaia.dev, then sign in as Bob to show
     "Shared with me"; show Carol has no access.
  4. What works end to end + what you intentionally deprioritized
     (real-time collab, real auth).
  5. Key implementation decisions (TipTap, Prisma, server-side access control).
  6. How AI supported the workflow (see AI_WORKFLOW.md).
